# Backlog

## Remaining Item

1. Update `cat-fact/README.md` to reflect the monorepo module paths.
   - Current README still references old task paths like `:androidApp`, `:desktopApp`, and `:webApp`.
   - It should use `:cat-fact:androidApp`, `:cat-fact:desktopApp`, and `:cat-fact:webApp`.
   - Verify iOS path wording is consistent with the moved location under `cat-fact/iosApp`.
